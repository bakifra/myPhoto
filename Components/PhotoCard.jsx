const React = require("react");

function PhotoPage({ photo }) {
  return (
    <div>
        {/* <img src={photo.url}> </img> */}
        <btn> del </btn>
        <btn> fav </btn>
    </div>
  );
}
module.exports = PhotoPage; 